## Timeout during `vagrant up`

If you get a timeout on `vagrant up`, run `vagrant provision`, after confirming in VM that the machine is up and running. See issue #55 for suggested fix that needs a developer.
